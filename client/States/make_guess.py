import pygame as pg
from Constants.constants import *
from backend_handler import make_guess

time = 0 

def draw(game, screen):

def update(game, events):
    global time
    if game.flicker:
        time = pg.time.get_ticks()
    return "make_guess"
